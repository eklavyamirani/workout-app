services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: localdev

    volumes:
      - ~/.claude:/home/vscode/.claude:cached
      - ~/.claude.json:/home/vscode/.claude.json:cached

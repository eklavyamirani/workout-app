# Authentication Configuration
# Set to 'true' to enable Supabase authentication in staging/production
# Set to 'false' or leave unset for local development (uses no-op adapter)
VITE_ENABLE_AUTH=false

# Supabase Configuration (required when VITE_ENABLE_AUTH=true)
# Uncomment and fill in these values for staging/production deployments
# Get these from your Supabase project settings: https://app.supabase.com

# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Note: In local development with VITE_ENABLE_AUTH=false, the app will use
# localStorage-only mode with a mock authentication layer (no-op adapter).
# No Supabase credentials are needed for local development.
